import 'package:flutter/material.dart';

class AdaptiveBackButton extends StatelessWidget {
  const AdaptiveBackButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
