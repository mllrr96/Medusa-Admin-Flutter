enum SortOptions {
  aZ,
  zA,
  priceHigher,
  priceLower,
}

enum ViewOptions {
  list,
  grid,
}

enum AppearanceMode {
  device,
  light,
  dark,
}

enum OrderStatus {
  delivered,
  canceled,
  upcoming,
}
