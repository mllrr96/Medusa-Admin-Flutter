enum SortOptions {
  aZ,
  zA,
  priceHigher,
  priceLower,
}

enum ViewOptions {
  list,
  grid,
}

enum AppearanceMode {
  device,
  light,
  dark,
}

enum ProductComponents {
  generalInfo,
  salesChannel,
  addVariant,
  editVariants,
  editOptions,
  editAttributes,
  editThumbnail,
  editMedia,
}
